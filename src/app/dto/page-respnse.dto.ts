export interface PageResponse {
    size: number;              // Equivalente a int size
    totalPages: number;         // Equivalente a int totalPages
    totalElements: number;      // Equivalente a int totalElements
    number: number;             // Equivalente a int number
    numberOfElements: number;   // Equivalente a int numberOfElements
    first: boolean;            // Equivalente a boolean first
    last: boolean;             // Equivalente a boolean last
    empty: boolean;   
  }